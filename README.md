# Game Hub MVP

Vite + React + TypeScript hub app lives at `apps/web`.

## Local development

```bash
cd apps/web
npm install
npm run dev
```

## Build and preview

```bash
cd apps/web
npm run build
npm run preview
```

## Docker

```bash
docker compose up --build
```

Then open `http://localhost:8080`.
